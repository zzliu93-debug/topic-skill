---
name: topic-skill
description: 按需在当前 Obsidian 知识库生成全量收录 AIHOT 精选、合并近 30 天讨论并按讨论触发度排序的 AI 日报。适用于“运行 topic-skill”“生成今天的 AI 日报”“更新今日 AI 资讯”“补跑今天早上没执行的日报”等请求。
---

# AI 日报工作流

把一次调用视为一次完整日报交付。默认处理执行当天，时区固定为 `Asia/Shanghai`。不生成选题、素材包、口播稿或每周复盘，也不修改定时任务。

## 1. 确定知识库与日期

1. 优先把当前工作目录中含 `.obsidian` 的目录作为知识库根目录。
2. 若当前目录没有 `.obsidian`，查找当前目录下的 `AI资讯/AI资讯总览.md`。
3. 若仍无法确定根目录，先询问用户，不要猜测或写入其他位置。
4. 默认运行日期为北京时间当天。用户指定日期时，必须说明数据源能否覆盖该日期；无法可靠覆盖时不要伪造历史热点。
5. 只读取和写入知识库的 `AI资讯` 目录，不匹配其他旧笔记。绝不创建 `AI资讯/待观察`。

## 2. 收集可验证信息

先调用 `last30days`，再调用 `aihot`。两者都必须先读取并遵循其各自的 `SKILL.md`。

### last30days

- 路径通常为 `C:/Users/32834/.codex/skills/last30days/SKILL.md`。
- **先运行 `last30days.py --diagnose`，再决定是否跳过。** 这是安全诊断：不得读取浏览器 Cookie 值、不得修改配置。不要只凭某个 `.env` 文件是否存在、是否显示可选 API Key，或一次 `grep` 结果判断“未初始化”。
- 按诊断结果严格分流：
  - `permission_preflight.status` 为 `ready` 且 `available_sources` 非空：基础研究可用，必须完整执行该 Skill 的发现和研究协议；即使 X、YouTube、TikTok、ScrapeCreators 或其他可选提供方未配置，也**不能**跳过。
  - 基础研究可用但可选来源缺失：在成品中称为“可选来源覆盖缺口”，明确只列出实际返回的平台；绝不能称为“未初始化”或“last30days 不可用”。
  - 只有当该 Skill 的首跑检测明确要求引导式初始化，或 `--diagnose` 失败且无可用来源时，才称为“需要初始化/不可用”。此时不读取 Cookie、不静默执行 setup；继续完成 AIHOT 部分，并准确说明缺口。
- 诊断与实际研究不一致时，以实际研究的 `source_status` 为准。只把实际返回的平台写成来源；不可把未连接的 X、YouTube、TikTok 等平台描述为已验证热度。
- 基础研究可用时，完整执行该 Skill 的发现和研究协议，关注近 30 天社区对 AI 工具、工作流、痛点和教程的真实讨论。
- 按该 Skill 的要求，把研究缓存保存在它自己的记忆目录，不把原始缓存写进知识库。

### aihot

- 路径通常为 `C:/Users/32834/.agents/skills/aihot/SKILL.md`。
- 用 AIHOT 获取当前 AI 新闻和事件，遵守其只读接口、请求头和时间处理要求。
- 日报窗口为北京时间“昨日 00:00 至执行时刻”；先请求可用窗口，再在本地按时间筛选。
- 分页取完该窗口内的所有 `selected` 条目，不得因热度、篇幅或与其他新闻相似而遗漏。分页不可用或请求不完整时，明确停止日报写入并说明缺口，不能把不完整结果伪装成全量。
- 建立“AIHOT 精选条目 → 日报事件簇”的逐条映射。完全相同的重复报道可并入同一事件簇，但该簇的“发生了什么事”必须分别交代每条精选提供的不同事实；映射数必须等于本次获取的精选总数。
- 将 `last30days` 返回的近期讨论与上述事件簇按同一事实对象合并。没有对应精选、但讨论信号充分且事实可验证的近期事件也可独立成簇。不得凭模型记忆补写当前新闻。

### 日报排序

日报的顺序只按事件引发读者评论或讨论的可能性从高到低排列，不沿用选题推荐度。排序时综合近期直接互动和跨社区讨论、事件的新鲜度与影响范围、争议或不确定性、以及普通使用者的相关性；`last30days` 的实际互动数据优先于主观判断。分数只作内部排序依据，不在日报中展示虚构的概率、热度或来源。

## 3. 写入日报

创建缺失的合法目录，并按 `references/output-spec.md` 生成和校验以下文件：

1. `AI资讯/日报/YYYY-MM-DD-AI简报.md`
2. 更新 `AI资讯/AI资讯总览.md` 的当日简报链接，不破坏已有人工内容。

已有同名文件时，先读取文件；只更新本次工作流管理的日期区块或整份当日产物，避免覆盖无关人工笔记。

## 4. 交付前检查

逐项确认：日期与北京时间正确；AIHOT 所有精选均已映射到一个日报事件簇，映射数与精选总数相等；日报事件簇已和 `last30days` 合并去重，并按讨论触发可能性降序；日报正文不出现来源、平台名、原始链接、时间戳或评分，只包含“发生了什么事”和“值得关注什么”；没有创建“待观察”；日报和索引均已落盘。最后向用户简要报告文件位置和来源覆盖情况。
